import { QouteDirective } from './qoute.directive';

describe('QouteDirective', () => {
  it('should create an instance', () => {
    const directive = new QouteDirective();
    expect(directive).toBeTruthy();
  });
});
